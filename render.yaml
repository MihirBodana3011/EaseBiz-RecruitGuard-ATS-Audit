services:
  - type: web
    name: easebiz-recruitguard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k gevent --timeout 300 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
